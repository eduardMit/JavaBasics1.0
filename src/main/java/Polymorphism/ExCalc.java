package Polymorphism;

public class ExCalc {
/*
    // Using Polymorphism, provide the option to calculate the sum of 2 numbers, first integers and then doubles
    // Use that option in a different class. Provide values for each case, and print the results

    class NumberCalculator {
        double sum(double a, double b) {
            return a + b;
        }
    }

    class IntegerCalculator extends NumberCalculator {
        @Override
        double sum(double a, double b) {
            int sum = (int) a + (int) b;
            return sum;
        }
    }

    class Main {
        public static void main(String[] args) {
            NumberCalculator calculator;

            // Calculate sum of two integers
            calculator = new IntegerCalculator();
            int result1 = (int) calculator.sum(3, 4);
            System.out.println("Sum of integers: " + result1);

            // Calculate sum of two doubles
            calculator = new NumberCalculator();
            double result2 = calculator.sum(2.5, 3.7);
            System.out.println("Sum of doubles: " + result2);

*/
        }
